{
  "build": { "builder": "nixpacks" },
  "deploy": {
    "startCommand": "DISABLE_FINBERT=1 streamlit run dashboard/app.py --server.port $PORT --server.address 0.0.0.0",
    "restartPolicyType": "always"
  }
}
